const newMatchStyles = theme => ({
  dialogContent: {
    overflowY: `hidden`
  },
  tabContainer: {
    height: `80vh`,
    overflowY: `auto`
  },
  title: {
    flexGrow: 1
  }
});

export default newMatchStyles;
